import "./footer.scss";

const Footer = () => {
  return (
    <footer className="footer">
      <nav>
        <a href="#">Accueil</a>
        <a href="#">Jeux</a>
        <a href="#">Contact</a>
      </nav>
    </footer>
  );
};

export default Footer;
